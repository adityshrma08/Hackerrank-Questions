#include<bits/stdc++.h>
using namespace std;


int main() {
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */   
    
    int N;
    cin >> N;
    int a;
    
    vector<int>v;
    for(int i = 0; i<N; i++){
        cin >> a;
        v.push_back(a);
    }
    
    sort(v.begin(),v.end());
    
    for(auto a: v)
    cout << a << " ";
    
    
    return 0;
}
